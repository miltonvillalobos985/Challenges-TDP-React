import LoginForm from "../LoginForm";

const Login = () => {
  if (false) {
    return (
      <div>
        Hello User <button onClick={() => {}}>Login</button>
      </div>
    );
  }

  return <LoginForm />;
};

export default Login;
